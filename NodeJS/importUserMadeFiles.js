const math = require('./math')
// const {PI , square}=require('./math') This is also valid

// This will just return an empty object if the usermade file has not defined any exports
console.log(math);
console.log(math.square(9));